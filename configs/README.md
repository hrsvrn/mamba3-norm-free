# Configs

- `data/`: dataset build configs and frozen dataset manifests.
- `model/`: future model-size and architecture configs.
- `train/`: future optimizer/schedule/runtime configs.
- `experiments/`: future named ablation bundles.
